My parent is preferred : <%= is_preferred %>.

<% @successors.each{|successor| %>

  <% is_preferred_successor = (successor.id < 30) %>

  <%= render :partial => 'tests3/timeline', :object => successor, :locals => {:is_preferred => is_preferred_successor} %>

<% } %>